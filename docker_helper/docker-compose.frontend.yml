# ═══════════════════════════════════════════════════════════════════════════════
# Nivo — Solo Frontend
# ═══════════════════════════════════════════════════════════════════════════════
#
# Levanta únicamente el contenedor del frontend React.
# El navegador hablará directamente con la Base URL configurada en la app.
#
# Uso:
#   docker compose -f docker_helper/docker-compose.frontend.yml up --build
#   docker compose -f docker_helper/docker-compose.frontend.yml down
#
# Servicios:
#   • frontend   → http://localhost:3000
#
# NOTA: Ajusta VITE_API_BASE_URL al ambiente que quieras consumir.
# ═══════════════════════════════════════════════════════════════════════════════

services:

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8001
        VITE_APP_NAME: Nivo API Console
        VITE_ENVIRONMENT: development
    container_name: nivo-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/nginx-health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
